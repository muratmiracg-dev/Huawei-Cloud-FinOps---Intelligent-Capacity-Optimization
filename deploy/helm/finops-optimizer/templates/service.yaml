apiVersion: v1
kind: Service
metadata:
  name: {{ include "finops-optimizer.fullname" . }}
  labels:
    {{- include "finops-optimizer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "finops-optimizer.selectorLabels" . | nindent 4 }}
