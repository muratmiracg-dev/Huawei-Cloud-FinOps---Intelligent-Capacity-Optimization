1. Port-forward the API:
   kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "finops-optimizer.fullname" . }} 8080:{{ .Values.service.port }}

2. Open the interactive API:
   http://127.0.0.1:8080/docs

3. Query the executive summary:
   curl http://127.0.0.1:8080/api/v1/summary
