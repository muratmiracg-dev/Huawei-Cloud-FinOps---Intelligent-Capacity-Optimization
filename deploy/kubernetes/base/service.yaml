apiVersion: v1
kind: Service
metadata:
  name: finops-optimizer
  namespace: finops-system
  labels:
    app.kubernetes.io/name: finops-optimizer
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: finops-optimizer
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
